---
title: "Reasonara"
description: "Ingest documents, ask questions, explore memories, and configure the knowledge base."
---

Reasonara is the admin interface for Cielara's knowledge base (Agent Memory). You can upload documents, manage collections, ask questions against your ingested content, and configure retrieval settings. Click **Reasonara** in the sidebar to open the page.

<Info>
  Reasonara is feature-flagged and may not be visible in all deployments. When enabled, it appears in the sidebar under the Reasonara group with the description "Agent memory & knowledge."
</Info>

## Collections

A collection dropdown at the top-left lets you organize knowledge into separate namespaces. The default collection is called "default." Your active collection is saved between sessions.

To delete a collection, click the red trash icon next to the dropdown. This permanently removes the collection and all its documents, segments, and memories. The "default" collection cannot be deleted.

## Stats

Three cards at the top-right show counts for the active collection: Documents, Segments, and Memories. A gear icon next to the stats opens the [settings drawer](#settings).

## Uploading Documents

1. Click the file picker labeled **Upload documents (MD, DOCX, CSV, PDF)**. You can select multiple files.
2. Click **Ingest N file(s)**. The button changes to "Queuing..." while files are submitted.

Each file shows a progress row with its name and a status chip:

| Stage | Status label |
|---|---|
| Waiting | Queued |
| Converting format | Normalizing |
| Splitting into chunks | Segmenting |
| Extracting knowledge | Extracting memories |
| Duplicate detected | Already ingested |
| Finished | Done, N memories |
| Error | The error message |

A progress bar runs under each row. Completed files clear on their own; failed ones stay up long enough to read the error.

## Knowledge Base Table

Below the upload area, a table shows all documents in the active collection. Columns: Name, Data Source, Type, Segments, and Ingested (timestamp). Use the search field to filter by name. Click a row to preview the document's Markdown content in a dialog. Click the red trash icon on a row to delete that document and all its segments and memories.

Data source labels map to friendly names: Google Drive, SharePoint, Canva, GitHub, Notion, Slack, Jira, Confluence, PagerDuty, or Upload.

While files are still being ingested, the table refreshes on its own.

## Data Sources

A card for each connected data source shows whether it is configured, how many documents have been ingested from it, and whether a sync is currently running.

Click **+ Connect** on any card to go to the Connections page with the right tab pre-selected.

## Asking Questions

Below a divider, the "Ask a Question" section lets you query your knowledge base.

1. Type a question in the input field.
2. Adjust the **Memories to retrieve** slider (1 to 20, default 5) to control how much context the answer draws on.
3. Optionally check **Include Source Segments** to see the raw text the answer was built from.
4. Click **Ask**.

The ask is asynchronous. If you navigate away and come back, a pending or completed result is restored from your session.

### Results

When the answer is ready, three sections appear:

The **answer** renders as Markdown, with chips summarizing how many memories it drew on and how many retrieval steps it took.

Below the answer, a **Graph View** tab shows the retrieval context visually, and a **Raw JSON** tab shows the full response for debugging.

An expandable **Retrieved Results** section lets you inspect what the answer was built from: the memories themselves, the segments they came from, and the source documents.

### Memory Graph

The Graph View tab shows how an answer was assembled, in four layers:

| Layer | What it represents |
|---|---|
| Document | A source document you ingested |
| Segment | A chunk of text split out of that document |
| Memory | An individual fact extracted from a segment |
| Cue anchor | A concept that memories are indexed against for retrieval |

Edges run down the layers, from documents to their segments to the memories extracted from them. Further edges link memories that share an anchor, sit in the same document, or are related hierarchically. Hover a node to highlight it and its immediate neighbors.

## Settings

Click the gear icon next to the stats cards to open the settings drawer. It has five collapsible sections:

### LLM models

| Setting | Options |
|---|---|
| Agent Model | gpt-4.1, gpt-4.1-mini, gpt-4.1-nano, gpt-5.4-mini, gpt-5-nano |
| QA Model | Same options as Agent Model |
| QA Temperature | Slider, 0.0 to 1.0, step 0.1 |

### Embedding

| Setting | Options |
|---|---|
| Embedding Model | text-embedding-3-small, text-embedding-3-large |

### Retrieval

Cap how much work a single query does. **Budget type** picks what you are limiting (reasoning steps, characters retrieved, or number of results), and **Max steps**, **Max chars**, and **Top K** set the ceiling for each. Raising these produces more thorough answers at the cost of speed.

### Hybrid search

Retrieval combines semantic search, keyword search, and cue anchors. Toggle each on or off, and use **RRF k** to control how heavily the combined ranking favors results that scored well in more than one method.

### Segmentation

**Chunk size** and **Overlap** control how documents are split before extraction. Larger chunks preserve more context per segment; more overlap reduces the chance a fact is cut in half at a boundary. Changing these only affects documents ingested afterwards.

Click **Apply Settings** to save. Changes take effect for future queries and ingestions.
